# Local Route Builder

The local web UI saves user-created route files in:

```text
load-testing/routes/local/
```

Those files are ignored by git because they can describe private API flows.
Commit only generic examples or documentation, not real production tokens,
private routes, or generated reports.
